#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../crojsdoc/bin/crojsdoc" "$@"
  ret=$?
else 
  node  "$basedir/../crojsdoc/bin/crojsdoc" "$@"
  ret=$?
fi
exit $ret
